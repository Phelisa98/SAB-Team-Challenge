import pandas as pd
import numpy as np

#Cleaning data = 4 Step process
#1. Find missing data (pd.isnull(pd.df))

df = pd.read_excel('Tech Stream Dataset_Amended.xlsx')

df = df.drop_duplicates()

df['Volume'] = df['Volume'].abs()

#new_df = df.dropna()

#print(new_df.to_string())
